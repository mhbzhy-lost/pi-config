import assert from "node:assert/strict";
import test from "node:test";
import { createRepairChallenge, deriveFindingFromFailedEvidence, issueRepairCapability, rejectSubjectHash, repairEpisodeTransition, validateRemediationTask } from "../scripts/lib/goal-engine/repair-policy.mjs";
const hash = (c) => c.repeat(64);
function projection() { const p = { goalId: "goal-1", executionRevision: 3, executionContractHash: hash("c"), writePolicy: { allowedPaths: ["src/**", "test/**"] }, conditions: new Map([["c-1", { definition: { remediation: { policy: "autonomous", allowed_paths: ["src/fix/**", "test/fix/**"] } } }]]), findings: new Map(), repairEpisodes: new Map(), repairChallenges: new Map(), tasks: new Map(), observationRuns: new Map(), evidenceHistory: [] }; p.observationRuns.set("run-1", { phase: "recorded", evidenceId: hash("a"), conditionId: "c-1" }); p.evidenceHistory.push({ run: { runId: "run-1" }, evidenceId: hash("a"), conditionId: "c-1", executionRevision: 3, executionContractHash: hash("c"), verdict: { kind: "failed", findingFingerprint: hash("b") } }); return p; }
function task(paths = ["src/fix/a.mjs"]) { return { description: "修复失败条件", deps: [], writePaths: paths, acceptance: { criteria: [{ id: "reverify", statement: "通过复验", evidenceKinds: ["tests"] }] }, workflow: "tdd" }; }
test("finding is derived only from current failed ledger references and is idempotent", () => { const p = projection(); const first = deriveFindingFromFailedEvidence({ projection: p, runId: "run-1", evidenceId: hash("a") }); assert.deepEqual(first.events[0].data, { findingId: first.finding.findingId, conditionId: "c-1", runId: "run-1", evidenceId: hash("a"), fingerprint: hash("b") }); p.findings.set(first.finding.findingId, first.finding); assert.equal(deriveFindingFromFailedEvidence({ projection: p, runId: "run-1", evidenceId: hash("a") }).events.length, 0); assert.throws(() => deriveFindingFromFailedEvidence({ projection: p, runId: "run-1", evidenceId: hash("d") }), /ledger/); });
test("ordinary metadata is rejected and remediation scope and durable user approval are strict", () => { const p = projection(); p.repairEpisodes.set("ep-1", { episodeId: "ep-1", conditionId: "c-1", findingIds: ["f-1"], remediationTaskIds: [], status: "active" }); p.findings.set("f-1", { findingId: "f-1", conditionId: "c-1", executionRevision: 3, status: "repairing", episodeId: "ep-1" }); assert.throws(() => validateRemediationTask({ projection: p, episodeId: "ep-1", findingIds: ["f-1"], taskDef: { ...task(), metadata: {} } }), /metadata/); assert.throws(() => validateRemediationTask({ projection: p, episodeId: "ep-1", findingIds: ["f-1"], taskDef: task(["src/nope.mjs"]) }), /scope/); const planned = validateRemediationTask({ projection: p, episodeId: "ep-1", findingIds: ["f-1"], taskDef: task() }); assert.equal(planned.taskDef.metadata.kind, "remediation"); assert.deepEqual(planned.events.map((entry) => entry.type), ["goal.amended", "repair.task_linked"]); p.conditions.get("c-1").definition.remediation.policy = "user-approved"; assert.throws(() => validateRemediationTask({ projection: p, episodeId: "ep-1", findingIds: ["f-1"], taskDef: task(), capability: {} }), /capability/); });
test("multiple tasks wait for every accepted task and cancellation permits persisted debt", () => { const p = projection(); p.repairEpisodes.set("ep-1", { episodeId: "ep-1", conditionId: "c-1", findingIds: ["f-1"], remediationTaskIds: ["t-1", "t-2"], status: "waiting_for_tasks" }); p.tasks.set("t-1", { status: "accepted" }); p.tasks.set("t-2", { status: "succeeded" }); assert.deepEqual(repairEpisodeTransition({ projection: p, episodeId: "ep-1", event: { type: "task.accepted", taskId: "t-1" } }).events, []); p.tasks.get("t-2").status = "accepted"; const plan = repairEpisodeTransition({ projection: p, episodeId: "ep-1", event: { type: "task.accepted", taskId: "t-2" } }); assert.equal(plan.events[0].type, "repair.reverification_requested"); p.tasks.get("t-1").status = "pending"; p.tasks.get("t-1").attempts = 0; p.tasks.get("t-2").status = "pending"; p.tasks.get("t-2").attempts = 0; const h = hash("e"), refs = { ownedTaskIds: ["t-1", "t-2"], ownedRunIds: [], terminalProofRefs: [], workspaceClosureProofRefs: [{ taskId: "t-1", proofHash: h, disposition: "never_started", released: true }, { taskId: "t-2", proofHash: h, disposition: "never_started", released: true }], resourceClosureProofRefs: [], resourceDebt: false }; assert.equal(repairEpisodeTransition({ projection: p, episodeId: "ep-1", event: { type: "cancel", cancellation: refs } }).events[0].type, "repair.episode_cancel_requested"); });
test("unlinked and non-PASS recorded observations cannot resolve a reverifying Episode", () => {
  const p = projection(); p.repairEpisodes.set("ep-1", { episodeId: "ep-1", conditionId: "c-1", findingIds: ["f-1"], remediationTaskIds: [], ownedRunIds: ["owned"], status: "reverifying" }); p.findings.set("f-1", { findingId: "f-1", conditionId: "c-1", status: "reverification" });
  p.observationRuns.set("unlinked", { runId: "unlinked", conditionId: "c-1", phase: "recorded", evidenceId: hash("a") }); p.evidenceHistory.push({ run: { runId: "unlinked" }, evidenceId: hash("a"), conditionId: "c-1", verdict: { kind: "passed" } });
  assert.deepEqual(repairEpisodeTransition({ projection: p, episodeId: "ep-1", event: { type: "condition.observation_recorded", conditionId: "c-1", runId: "unlinked", evidenceId: hash("a") } }).events, []);
  for (const kind of ["failed", "inconclusive", "infrastructure_error"]) { p.observationRuns.set(`owned-${kind}`, { runId: `owned-${kind}`, conditionId: "c-1", phase: "recorded", evidenceId: hash("b") }); p.evidenceHistory.push({ run: { runId: `owned-${kind}` }, evidenceId: hash("b"), conditionId: "c-1", verdict: { kind } }); p.repairEpisodes.get("ep-1").ownedRunIds.push(`owned-${kind}`); assert.deepEqual(repairEpisodeTransition({ projection: p, episodeId: "ep-1", event: { type: "condition.observation_recorded", conditionId: "c-1", runId: `owned-${kind}`, evidenceId: hash("b") } }).events, [], kind); }
});

test("challenge plan never persists nonce plaintext", () => { const p = projection(); p.repairEpisodes.set("ep-1", { episodeId: "ep-1", conditionId: "c-1", findingIds: ["f-1"], remediationTaskIds: [], status: "active" }); const c = createRepairChallenge({ projection: p, episodeId: "ep-1", action: "reject", sessionId: "s-1", requestedAt: 1, expiresAt: 2, subjectHash: rejectSubjectHash(p, p.repairEpisodes.get("ep-1")) }); assert.equal(JSON.stringify(c).includes("nonce"), false); assert.equal(JSON.stringify(c.events).includes("nonceDigest"), false); });
test("capability is minted only from a durable approved challenge and plans atomic consume", () => { const p = projection(); const episode = { episodeId: "ep-1", conditionId: "c-1", findingIds: ["f-1"], remediationTaskIds: [], status: "active" }; p.repairEpisodes.set("ep-1", episode); const subjectHash = rejectSubjectHash(p, episode); p.repairChallenges.set("ch-1", { challengeId: "ch-1", executionRevision: 3, episodeId: "ep-1", action: "reject", subjectHash, sessionId: "s-1", userEntryId: "u-1", phase: "approved", expiresAt: 20 }); const capability = issueRepairCapability({ projection: p, challengeId: "ch-1", now: 10 }); assert.equal(capability.nonce.length > 0, true); const plan = repairEpisodeTransition({ projection: p, episodeId: "ep-1", event: { type: "repair.reject", capability, consumedAt: 10 } }); assert.deepEqual(plan.events.map((event) => event.type), ["repair.capability_consumed", "repair.episode_rejected_by_user"]); assert.equal(JSON.stringify(plan).includes(capability.nonce), false); });
