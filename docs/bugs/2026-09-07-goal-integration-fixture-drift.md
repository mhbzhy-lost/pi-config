# Goal integration fixture drift (2026-09-07)

## Classification

| Fixture class | Entry and identity | Required event/service order | First divergence | Resolution |
| --- | --- | --- | --- | --- |
| Root Broker rich terminal status | `subagent-managed-worktree.integration.mjs`; Goal `fixture-goal`, task `unified-workspace`, ticket/workspace `broker-proof-workspace`, leaf `leaf-run` | Host ticket -> managed allocation -> coordinator allocation/confirmation -> Host authorization -> workspace bind -> coordinator binding -> official terminal observation | The standalone coding facade registered a non-Goal authorization. `inspectExecutionProof()` therefore correctly returned `terminal: null`; the test dereferenced `proofId`. | A bounded Host coordinator fixture now issues a `goal-run-binding-ticket.v2`; the public facade creates the authorized, Goal-bound owned run. The terminal fact remains from `broker.observeTerminal`, not a status/caller proof. |
| Binding sidecar / Broker registry | `goal-engine-executor-binding.integration.mjs`; each `integratedFixture` has its own root Host and arena async directory | fixture Broker bind -> ticket allocation/confirmation -> public coding spawn authorization/binding -> started observation -> terminal/sidecar read | Two test Brokers were bound to one `pi`, causing `Root subagent broker is already bound` before the sidecar path was reached. Their hand-selected lifecycle session and PID also differed from the spawned binding. | Reuse the fixture-owned Broker and exact `root-session-1`/PID binding. Per-test arena async directories are released by fixture teardown; no child polling or sleep is used. |
| Goal workspace receipt/path | `goal-engine-extension.integration.mjs` and `goal-workspace-service-fixture.mjs`; owner identity is `(goalId, taskId, attempt)` | coordinator ticket -> `ensureAllocated` receipt -> `workspaceAllocated` -> `confirmSpawn` -> `bindSpawn` | Legacy helpers assumed an automatically projected workspace; current service exposes its receipt/inventory as the authority. | Existing helper allocates, loads, inspects, and releases only through managed-workspace service APIs; focused service/status cases pass. |
| Settlement `expectedCriteria` | Goal extension invoke harness; identity is task executor binding and settlement head | canonical run criteria -> executor evidence fingerprint -> coordinator predicates evaluated separately | Fixture included coordinator criteria in executor evidence, allowing duplicate/evaluator-boundary drift. | Harness filters `evaluator === "coordinator"`, asserts unique canonical run IDs, and fingerprints/serializes that same list. |

No production defect was established: all first deviations occurred before the relevant public Host/Broker/workspace API boundary or in test-only evidence construction. Production sources were not modified.
